func main() -> int
then
    struct person then
        string name;
        int age;
        string uid;
        bool is_employed;
    end

    struct person liam;

    bool is_cool := false;
    is_cool := true;

    liam.name := "Liam";

    return 0;
end
