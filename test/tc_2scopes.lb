int a := 5;
string b := "hello";
bool c := true;
float d := 3.14;

'a := 4 - (0 * 3.1);
a := a * 2;

'string b := "goodbye";

func fib(int n) -> int
then

'    n := nil;
'    println(n);
'    return nil;
end

func someFunction(int x, int y, bool z) -> void
then
    a := a + x;
    y := y - a;

    'if ((x + y) > 0) then
    '    z := true;
    'end

    bool ret := z;

    'println(ret);

    return ret;
end

'func fib(int x) -> float
'then
'    if ((x == 0) or (x == 1)) then
'        return x;
'    else then
'        return fib(x - 1) + fib(x - 2);
'    end
'end
