func main(int argc, string argv) -> int
then
    int x;

    if (x < 1) then
        x := 1;
    else then
        if (x > 1) then
            string foo := "bar";
            string bar := "baz";

            bar := bar + foo;
        end
    end

    bool eq := (0 == 0);
    bool gt := (1 > 0);
    bool lt := (0 < 1);
    bool ge := (1 >= 0);
    bool le := (-10000.2149 <= 0);
    bool ne := (false != true);

'    println("Hello world!");

'    return 0;
end
